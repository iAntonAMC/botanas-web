<footer class="site-footer">
  <div class="footer-cutouts" aria-hidden="true">
    <i></i><i></i><i></i><i></i><i></i><i></i>
  </div>
  <div class="footer-inner">
    <section class="footer-brand" aria-label="Botanas Lokas">
      <a class="footer-logo" href="/" aria-label="Botanas Lokas, inicio"
        ><span aria-hidden="true">🌶</span><b>Botanas<br /><em>Lokas</em></b></a
      >
      <p>Botanas con mucho crunch, chilito y corazón mexicano.</p>
      <a class="footer-cta" href="#snacks"
        >Arma tu antojo <span aria-hidden="true">→</span></a
      >
    </section>

    <nav class="footer-links" aria-label="Explorar Botanas Lokas">
      <h2>Explora</h2>
      <a href="#snacks">Nuestras botanas</a>
      <a href="#about">Nuestra historia</a>
      <a href="#testimonials">Voces de la fiesta</a>
    </nav>

    <section class="footer-contact">
      <h2>¿Se te antojó?</h2>
      <p>Escríbenos y hagamos de tu próxima reunión una fiesta.</p>
      <a class="email" href="mailto:hola@botanaslokas.mx"
        >hola@botanaslokas.mx <span aria-hidden="true">↗</span></a
      >
      <div class="socials" aria-label="Redes sociales">
        <a href="#" aria-label="Instagram">ig</a><a
          href="#"
          aria-label="Facebook">f</a
        ><a href="#" aria-label="TikTok">tk</a>
      </div>
    </section>
  </div>
  <div class="footer-bottom">
    <p>
      © {new Date().getFullYear()} Botanas Lokas. Hecho para los que nunca dicen “ya
      no”.
    </p><a href="#">Privacidad</a>
  </div>
</footer>

<style>
  .site-footer {
    position: relative;
    overflow: hidden;
    border-top: 4px solid #1c1b1b;
    background: #ffd700;
    color: #1c1b1b;
  }
  .footer-cutouts {
    display: flex;
    height: 21px;
    background: #fcf9f8;
    overflow: hidden;
  }
  .footer-cutouts i {
    flex: 1;
    min-width: 72px;
    background: #e2007b;
    clip-path: polygon(0 0, 50% 100%, 100% 0);
  }
  .footer-cutouts i:nth-child(2n) {
    background: #75ff68;
  }
  .footer-cutouts i:nth-child(3n) {
    background: #b50061;
  }
  .footer-inner {
    display: grid;
    grid-template-columns: 1.25fr 0.7fr 1fr;
    gap: clamp(2.2rem, 7vw, 7rem);
    width: min(1280px, calc(100% - 2rem));
    margin: 0 auto;
    padding: clamp(3.2rem, 7vw, 5.3rem) 0 3rem;
  }
  .footer-brand p {
    max-width: 320px;
    margin: 1rem 0 1.35rem;
    font-size: 0.95rem;
    font-weight: 500;
    line-height: 1.55;
  }
  .footer-logo {
    display: inline-flex;
    align-items: center;
    gap: 0.7rem;
    color: inherit;
    text-decoration: none;
  }
  .footer-logo > span {
    display: grid;
    width: 50px;
    height: 50px;
    place-items: center;
    border: 2px solid #1c1b1b;
    border-radius: 50%;
    background: #fcf9f8;
    box-shadow: 3px 3px 0 #1c1b1b;
    font-size: 1.6rem;
    transform: rotate(-12deg);
  }
  .footer-logo b {
    font-family: "Anybody", sans-serif;
    font-size: 1.55rem;
    font-weight: 900;
    letter-spacing: -0.075em;
    line-height: 0.72;
  }
  .footer-logo em {
    color: #b50061;
    font-style: normal;
  }
  .footer-cta {
    display: inline-flex;
    gap: 0.7rem;
    padding: 0.75rem 0.95rem;
    border: 2px solid #1c1b1b;
    border-radius: 0.6rem;
    background: #b50061;
    box-shadow: 3px 3px 0 #1c1b1b;
    color: #fff;
    font-family: "Anybody", sans-serif;
    font-size: 0.8rem;
    font-weight: 800;
    text-decoration: none;
    transition:
      transform 150ms ease,
      box-shadow 150ms ease;
  }
  .footer-cta:hover {
    box-shadow: 0 0 0 #1c1b1b;
    transform: translate(3px, 3px);
  }
  h2 {
    margin: 0 0 1rem;
    font-family: "Anybody", sans-serif;
    font-size: 0.83rem;
    font-weight: 900;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }
  .footer-links {
    display: grid;
    align-content: start;
    gap: 0.6rem;
  }
  .footer-links h2 {
    margin-bottom: 0.35rem;
  }
  .footer-links a {
    width: fit-content;
    color: inherit;
    font-size: 0.93rem;
    font-weight: 600;
    text-decoration: none;
  }
  .footer-links a:hover {
    color: #b50061;
    text-decoration: underline 2px;
    text-underline-offset: 3px;
  }
  .footer-contact p {
    max-width: 285px;
    margin: 0 0 0.75rem;
    font-size: 0.93rem;
    line-height: 1.45;
  }
  .email {
    color: inherit;
    font-family: "Anybody", sans-serif;
    font-size: clamp(1rem, 2vw, 1.3rem);
    font-weight: 900;
    letter-spacing: -0.04em;
    text-decoration: underline 2px #1c1b1b;
    text-underline-offset: 5px;
  }
  .email:hover {
    color: #b50061;
  }
  .socials {
    display: flex;
    gap: 0.55rem;
    margin-top: 1.35rem;
  }
  .socials a {
    display: grid;
    width: 33px;
    height: 33px;
    place-items: center;
    border: 2px solid #1c1b1b;
    border-radius: 50%;
    background: #fcf9f8;
    box-shadow: 2px 2px 0 #1c1b1b;
    color: #1c1b1b;
    font-family: "Anybody", sans-serif;
    font-size: 0.68rem;
    font-weight: 900;
    text-decoration: none;
    transition:
      transform 150ms ease,
      box-shadow 150ms ease;
  }
  .socials a:hover {
    box-shadow: 0 0 0 #1c1b1b;
    transform: translate(2px, 2px);
  }
  .footer-bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    width: min(1280px, calc(100% - 2rem));
    margin: 0 auto;
    padding: 1.2rem 0;
    border-top: 2px solid rgba(28, 27, 27, 0.55);
    font-size: 0.72rem;
    font-weight: 600;
  }
  .footer-bottom p {
    margin: 0;
  }
  .footer-bottom a {
    color: inherit;
    text-decoration: underline;
    text-underline-offset: 3px;
  }
  @media (max-width: 720px) {
    .footer-inner {
      grid-template-columns: 1fr;
      gap: 2.2rem;
      padding-top: 3rem;
    }
    .footer-brand p {
      max-width: 360px;
    }
    .footer-bottom {
      align-items: flex-start;
      flex-direction: column;
      gap: 0.45rem;
    }
  }
  @media (prefers-reduced-motion: reduce) {
    .footer-cta,
    .socials a {
      transition-duration: 0.01ms;
    }
  }
</style>
